#pragma once

#include <stdio.h>

#include "data_structs.h"

/**
 * This function initiates the assembly process by calling the first cycle.
 * 
 * @param filename The name of the assembly file to process.
 */
void assemble(char* filename);
